# rocker/r-bspm, rocker/drd, rocker/r-devel, 

# compare on size, support of binary, number of preinstalled dependencies, version of R, which package management, 

# based on these properties, also check the performance for R scripts





def getImage(content):
    # TODO: check R version of user machine if we assume a running environment
    return 1


def getData():
    images = [
        "r-base"
    ]

getImage(1)





# docker run IMAGE

#  docker exec -it -u root c924abf79138 /bin/bash