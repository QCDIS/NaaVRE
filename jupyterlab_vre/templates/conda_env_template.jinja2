name: venv
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.7
  - pdal
  - python-pdal
  - gdal
{% if deps|length > 0 %}
  - pip
  - pip:
  {% for d in deps %}
    - {{ d }}
  {% endfor %}
{% endif %}
