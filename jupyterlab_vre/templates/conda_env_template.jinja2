name: venv
channels:
  - conda-forge
  - defaults
  - pytorch
  - fastai
dependencies:
  - pip
  - python>=3.8
{% for d in conda_deps %}
  - {{ d }}
{% endfor %}
{% if pip_deps|length > 0 %}
  - pip:
  {% for d in pip_deps %}
    - {{ d }}
  {% endfor %}
{% endif %}
